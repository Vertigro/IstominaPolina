nt(5) #выводит переменную на экран

for i in range(0,1,3): #Перебирает значение от 0 да 1 с шагом 3

if n==s: a = s #проверяет условие и если оно истинно выполняет условие 
elif: n!=f #вложенное условие, выполняемое, если выражение ложно
else: a = s #если условие ложно выполняет действие

input() #выводит переменную на экран

int(4) #меняет тип данных на целочисленный

str(45) #меняет тип данных на строковый

break #завершает выполнение программы

from turtle import #импортирует библиотеки python

forward(300)# движение черепахи вперёд 

backward(300)#движение черепахи назад

right(120)# поворот черевахи направо

left (120) #поворот черевахи налево

dot(5)#ставит точку с радиусом, указанном в скобках

goto(x*30,y*30)#осуществляет переход к определённой точке в коде программы

def f(): #задаёт функцию 
    f = 13 #функция
f #вывод функции

count('6')#возвращает количество раз, когда указанный элемент появляется в списке

from itertools import product
product('01234567',repeat=5)#возвращает декартово произведение входных итерируемых последовательностей 

c = []#создание списка

c.index('6') #находит значение индекса элемента в последовательности

c.len(5)#возвращает количество элементов(длину) в обьекте

c.split() #азделяет строку на список подстроек по разделителю

c.join(n)#преобразует список в строку

c.append(x)#добавляет в список один новый элемент

with open('24.txt') as f: #открывает текстовый фаил и выполняет с ними действия

readline() #острочно читает фаил

с.pop(0) #возвращает элемент, удаляя его из списка

max(kmax,cnt)#Выдаёт максимальное значение из 2

min(kmax,cnt)#Выдаёт минимальное значение из 2

replace('O','6')#производит замену символов в строке

s.sort(reverse = True)#аозвращает новый отсортированный список итерируемого объекта

# Алгоритм решеня задачи 9
#Нужно посчитать сколько раз число встречается в строке (СЧЕТЕСЛИ($:A1:$F1;A1)
#ЕСЛИ(СУММ(G1:L1)=8;1;0) //(тк у всех чисел длжно быть 1, а у одного 2, те сумма 6 чисел должна равняться 8)
#СРЗНАЧЕСЛИ(G1:L1;1; A1:f1) //(среденее значение(где ищем. что ищем, в каком диапозоне)
#СУММЕСЛИ(G1:L1;2;A1:f1)
#ЕСЛИ(n(M1=1;N1<=01);1;0) // n - номер столбца

#26 задача
#=ЕСЛИ(A1 - A2=0;0;A2)
#= C2 - C3
#=ЕСЛИ(E3 + D4>=3;0;E3 + D4)
#= СЧЁТЕСЛИ(E3:E6290;0) +1
